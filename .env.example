# Copy this file to .env.local – Next.js loads .env.local, not .env.example
# Supabase (required)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# App URL (for verification links in emails)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Email: Resend (https://resend.com)
# - Get API key from Resend Dashboard. With "onboarding@resend.dev" you can only send TO your Resend account email.
# - To send to any address: add and verify your domain in Resend, then set RESEND_FROM e.g. "Дупките на Ловеч <noreply@yourdomain.com>"
RESEND_API_KEY=re_xxxx
RESEND_FROM=Дупките на Ловеч <onboarding@resend.dev>

# If RESEND_API_KEY is empty, no email is sent; users can still verify via the link (e.g. from server logs or copy from success screen).

# Cron cleanup (optional): set a secret if you call /api/cron/cleanup with Authorization: Bearer <secret>
# CRON_SECRET=your-random-secret
